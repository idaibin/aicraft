interface:
  display_name: "Ask ChatGPT"
  short_description: "Ask ChatGPT for bounded external input"
  default_prompt: "Use $ask-chatgpt for requested ChatGPT input: infer the outcome, choose one theme and verified route, honor model preferences, send only with exact authorization, and verify the result locally."
