interface:
  display_name: "Code Security"
  short_description: "Review code and API changes for security risks"
  default_prompt: "Use $code-security to review code, API, auth, permission, config, dependency, logging, token, session, upload, or release changes for grounded security risks. Use $code-review first when the frontend/backend API contract chain is not already mapped."
