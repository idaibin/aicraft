interface:
  display_name: "Repository Delivery"
  short_description: "Classify, commit, and deliver reviewed Git changes"
  default_prompt: "Use $repo-delivery to classify reviewed Git changes by intent, commit each category unless one commit is requested, choose branch integration from evidence, and prove final refs."
