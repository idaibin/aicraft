interface:
  display_name: "Code Delivery"
  short_description: "Deliver reviewed Git changes and stop before PR creation"
  default_prompt: "Use $code-delivery to deliver reviewed local changes with path-limited staging, verification, local commit, current-branch push or sync, and no pull-request creation. Route explicit PR publishing to the available GitHub publishing workflow."
