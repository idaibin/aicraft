interface:
  display_name: "Ops Browser"
  short_description: "Operate browser sessions without disturbing user state"
  default_prompt: "Use $ops-browser to inspect and operate this page in the background when practical while preserving existing tabs, focus, and user state."
