interface:
  display_name: "Code Review"
  short_description: "Review dirty-tree ownership, contract chains, and commit plans"
  default_prompt: "Use $code-review to inspect existing local changes, classify ownership, review contract chains and validation, then propose semantic commit groups with exact path-limited staging. Do not commit unless explicitly asked."
