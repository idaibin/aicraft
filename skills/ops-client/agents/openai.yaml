interface:
  display_name: "Ops Client"
  short_description: "Operate and launch-review real desktop app windows"
  default_prompt: "Use $ops-client to identify the specified desktop client, confirm its startup command and runtime source, then inspect or operate the real app window without relying on a browser preview."
