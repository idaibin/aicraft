interface:
  display_name: "Ops Browser"
  short_description: "Operate browser surfaces with verified state and actions"
  default_prompt: "Use $ops-browser to select the smallest browser or DevTools operation, verify capability, identity, target, and authorization, then return before/after evidence."
