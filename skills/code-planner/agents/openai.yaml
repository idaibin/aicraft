interface:
  display_name: "Code Planner"
  short_description: "Plan code tasks with owned scope and verifiable gates"
  default_prompt: "Use $code-planner to split future codebase work into scoped, executable, verifiable task packages. Use subagents only when ownership is independent and auditable; route final dirty-tree or contract review to $code-review."
