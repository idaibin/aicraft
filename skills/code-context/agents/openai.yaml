interface:
  display_name: "Code Context"
  short_description: "Map codebase context and reusable surfaces"
  default_prompt: "Use $code-context when the user requests repository grounding, directory or documentation alignment, or a targeted reuse/interface inventory. Map real files, commands, project classes, and docs; for requested frontend or Rust inventories, identify reuse, extension, and reference candidates without implementing changes."
