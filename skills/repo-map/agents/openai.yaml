interface:
  display_name: "Repository Map"
  short_description: "Map project truth, reuse, and protocol authority routes"
  default_prompt: "Use $repo-map to resolve real roots and maintain the smallest semantic map of boundaries, commands, task routes, verified reuse, and requested protocol-authority chains with owner-bounded stale repair."
