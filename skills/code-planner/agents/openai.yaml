interface:
  display_name: "Code Planner"
  short_description: "Plan code tasks with owned scope and verifiable gates"
  default_prompt: "Use $code-planner to turn future codebase work into an executable, ownership-aware plan with explicit scope, dependencies, and validation gates."
