interface:
  display_name: "Frontend Audit"
  short_description: "Audit selected frontend framework and styling profiles"
  default_prompt: "Use $audit-frontend to run a read-only, profile-selected frontend audit and return evidence-ranked findings without editing code."
