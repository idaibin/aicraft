interface:
  display_name: "Repository Map"
  short_description: "Map Git or non-Git project truth and verified reuse routes"
  default_prompt: "Use $repo-map to resolve real project roots and maintain the smallest useful semantic map of boundaries, commands, task routes, and verified reuse."
