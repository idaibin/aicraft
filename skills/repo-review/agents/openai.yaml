interface:
  display_name: "Repository Review"
  short_description: "Review changes, specs, and protocol contract drift"
  default_prompt: "Use $repo-review to review a fixed local or immutable basis read-only across Standards, Spec, and applicable protocol-contract evidence, then return consolidated P0-P3 findings."
