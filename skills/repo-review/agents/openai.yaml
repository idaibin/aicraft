interface:
  display_name: "Repository Review"
  short_description: "Review worktrees, ranges, PRs, and releases read-only"
  default_prompt: "Use $repo-review to perform a read-only, basis-specific review of a worktree, snapshot, range, PR, release candidate, or verified package."
